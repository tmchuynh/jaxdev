/** @type {import('next').NextConfig} */
const nextConfig = {
      output: 'export',
      images: ['public/images'],
      reactStrictMode: true,
      basePath: '/jaxdev'
};

export default nextConfig;
